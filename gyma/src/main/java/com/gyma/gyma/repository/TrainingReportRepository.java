package com.gyma.gyma.repository;

import com.gyma.gyma.model.TrainingReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainingReportRepository extends JpaRepository<TrainingReport, Integer> {
}
